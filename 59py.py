#insert method
#to insert a new list ite, without replacing any of the existing values,
#we cabuse the insert() method.

#the imsert() methodinsert an item at the specified index:

#insert "jojo" as the third item?

bambo = ["vovo", "bibo","sabo"]
bambo.insert(0,"jojo")
print(bambo)