#get keys

#the keys() will return a list of all the keys in the dictionary.

#gwt a list of the key?

thisdict = {
    "brand": "ford",
    "model": "mustang",
    "year": 278
}
x = thisdict.keys()
print(x)
